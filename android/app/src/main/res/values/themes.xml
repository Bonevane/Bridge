<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Compose draws its own app bar, so the platform ActionBar must go, otherwise
      the title appears twice. Transparent bars let the app draw edge to edge.
    -->
    <style name="Theme.Bridge" parent="@android:style/Theme.DeviceDefault.DayNight">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">?android:attr/colorBackground</item>
    </style>
</resources>
